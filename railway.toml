[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "java -Dspring.profiles.active=prod -Dspring.data.mongodb.uri=$MONGODB_URI -jar app.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
